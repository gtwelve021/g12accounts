import Image from "next/image";
import Link from "next/link";
import { services, posts } from "@/content/site-data";
export function Eyebrow({ children }: { children: React.ReactNode }) { return <div className="eyebrow"><span />{children}</div>; }
export function Action({ href, children }: { href: string; children: React.ReactNode }) { return <Link className="editorial-action" href={href}>{children}<span aria-hidden="true">↗</span></Link>; }
export function ServiceDirectory() { return <section className="editorial-wrap service-directory" id="expertise"><div className="directory-intro" data-reveal><Eyebrow>02 / Expertise, connected</Eyebrow><h2>Complexity,<br /><em>made clear.</em></h2><p>Specialist expertise brought together around your business—not delivered in disconnected silos.</p><Action href="/contact-us/">Find your specialist</Action></div><div className="directory-rows">{services.map((service, i) => <Link data-reveal className="directory-row" href={`/${service.slug}/`} key={service.slug}><span className="row-number">0{i + 1}</span><div><h3>{service.label}</h3><p>{service.description}</p></div><span className="row-arrow" aria-hidden="true">↗</span></Link>)}</div></section>; }
export function Journal({ all = false }: { all?: boolean }) { return <section className="editorial-wrap journal"><div className="section-heading" data-reveal><div><Eyebrow>Perspectives / G12 Journal</Eyebrow><h2>Ahead of<br /><em>the curve.</em></h2></div>{!all && <Action href="/insights/">All perspectives</Action>}</div><div className="journal-list">{(all ? posts : posts.slice(3, 6)).map((post, i) => <Link className="journal-row" data-reveal href={`/${post.slug}/`} key={post.slug}><span className="row-number">{String(i + 1).padStart(2, "0")}</span><div className="journal-image"><Image src={i % 2 ? "/images/live/international-tax.jpg" : "/images/live/corporate-tax-advisory.jpg"} alt="" fill sizes="180px" /></div><div><small>{post.label}</small><h3>{post.title} {post.emphasis}</h3></div><span className="row-arrow" aria-hidden="true">↗</span></Link>)}</div></section>; }
