import { Eyebrow } from "@/components/editorial";
export function PageHero({ index, label, title, description }: { index: string; label: string; title: React.ReactNode; description: string }) { return <section className="interior-hero"><div data-hero-reveal><Eyebrow>{index} / {label}</Eyebrow><h1>{title}</h1></div><div className="interior-description" data-hero-reveal><span aria-hidden="true">↘</span><p>{description}</p></div></section>; }
